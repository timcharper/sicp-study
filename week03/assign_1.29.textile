h1. Notes on 1.29

* Integrand: A function used to calculate an integral
* quadratic equation
** Quadratic comes from quadratus, which is the Latin word for "square."

h2. polynomial

*Nomial* = name of a term

*Poly* = many

This is a polynomial

  3x^2 + 3x + 5
  
This is not, because of the 3/x (thats like 3 * x^(-1), instead of 3 * x)

  3x^2 + 3/x + 5

h2. Coefficient

It's a constant multiplicative factor of a specific object. In 3x^2, 3 is the coefficient.
